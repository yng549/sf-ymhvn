@isTest
public class BaseBatchHandlerTest {
	@isTest
    static void test()
    {
        //BaseBatchHandler obj = new BaseBatchHandler();
        //DataBase.executeBatch(obj); 
        //Database.executeBatch(new BaseBatchHandler(), 2000);
    }
}